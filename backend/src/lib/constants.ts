/** Supabase Storage bucket ids (must match migration). */
export const BUCKET_UPLOADS = "uploads" as const;
export const BUCKET_OUTPUTS = "outputs" as const;

export const DEFAULT_CSV_CONTENT_TYPE = "text/csv";
