/** Period format: `YYYY-MM` (e.g. `2026-03`). */
export declare function parsePeriodFolder(period: string): {
    year: string;
    month: string;
};
export declare function uploadObjectKey(uploadId: string, now?: Date): string;
export declare function outputPrefix(period: string, runId: string): string;
//# sourceMappingURL=paths.d.ts.map